<p>Welcome back to Stylii, <%= current_client.name %></p>

<% if @appointments.length > 0 %>
  <h2>Your future appointments:</h2>
  <ul>
    <% @appointments.each do |appointment| %>
    <% if appointment.start_time.future? %>
        <li>You will see <%= Stylist.find(appointment.stylist_id).name %> at <%= appointment.start_time.strftime("%I:%M%p on %d %b") %> | <%= link_to("Cancel", appointment, method: :delete, data: { confirm: 'Are you sure?' }) %> | <%= link_to("Change appointment", edit_appointment_path(appointment)) %></li>
    <% end %>
    <% end %>
  </ul>

  <h3>Past appointments:</h3>
    <ul>
      <% @appointments.each do |appointment| %>
        <% if appointment.start_time.past? %>
        <li>You saw <%= Stylist.find(appointment.stylist_id).name %> at <%= appointment.start_time.strftime("%I:%M%p on %d %b") %> | <% if appointment.stylii_rating.nil? %> button to rate <% else %> <%= appointment.rating %> <% end %></li>
        <% end %>

        <% end %>
    </ul>
  <% end %>
